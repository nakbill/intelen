{% extends 'base.html.twig' %}
{% block title %}Create New Country{% endblock %}
{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <h1 class="mb-4 text-center">Create New Country</h1>
                {{ form_start(form) }}
                {% include './forms/form-group.html.twig' with {'value': form.name, 'title': 'CountryName'} %}
                {{ form_widget(form._token) }} {# Render CSRF token field #}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
